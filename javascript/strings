strings
=======

# concatenate string
const url = `${this.heroesUrl}/${hero.id}`;
const url = this.heroesUrl + "/" + hero.id;

# check if a string contains a substring
let str = 'long_str1';
let substr = '_str1';
str.includes(substr);
---
'long_str1'.includes('_str1');

# check if a string starts with or ends with a substring
'long_str1'.startsWith('_str1') or 'long_str1'.endsWith('_str1');

# print a float with 2 decimal places
number1.toFixed(2)

